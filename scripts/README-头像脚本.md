# 人物头像脚本使用指南

本目录包含多个脚本，用于自动查找、下载和管理人物头像。

## 📋 脚本列表

### 1. `find-avatar-urls.js` - 生成配置文件
**用途**：生成一个配置文件模板，方便批量管理图片URL

**使用方法**：
```bash
node scripts/find-avatar-urls.js
```

**输出**：生成 `avatar-urls-config.json` 文件

---

### 2. `fetch-avatars-from-config.js` - 从配置文件下载
**用途**：读取配置文件中的URL并批量下载头像

**使用方法**：
1. 先运行 `find-avatar-urls.js` 生成配置文件
2. 编辑 `avatar-urls-config.json`，填写图片URL
3. 运行：
```bash
node scripts/fetch-avatars-from-config.js
```

**功能**：
- 自动下载图片到 `frontend/public/images/persons/`
- 自动更新 `persons.json` 文件
- 显示下载进度和统计

---

### 3. `fetch-avatars-simple.js` - 简单下载脚本
**用途**：直接在脚本中填写URL并下载

**使用方法**：
1. 编辑脚本，在 `avatarUrls` 对象中填写URL
2. 运行：
```bash
node scripts/fetch-avatars-simple.js
```

**示例**：
```javascript
const avatarUrls = {
  8: 'https://example.com/qinshihuang.jpg',
  17: 'https://example.com/caocao.jpg',
};
```

---

### 4. `add-avatar-urls.js` - 自动更新JSON
**用途**：检测已存在的图片文件，自动更新 `persons.json`

**使用方法**：
```bash
node scripts/add-avatar-urls.js
```

**功能**：
- 扫描图片目录
- 匹配人物ID和名称
- 自动添加 `avatarUrl` 字段

---

### 5. `check-avatars.js` - 检查状态
**用途**：检查当前头像添加进度

**使用方法**：
```bash
node scripts/check-avatars.js
```

**输出**：
- 图片目录状态
- 人物头像统计
- 已配置头像列表
- 无头像人物列表

---

## 🚀 快速开始

### 方法一：使用配置文件（推荐）

1. **生成配置文件**
   ```bash
   node scripts/find-avatar-urls.js
   ```

2. **填写图片URL**
   - 打开 `scripts/avatar-urls-config.json`
   - 访问 https://commons.wikimedia.org/ 搜索人物
   - 复制图片URL到配置文件中

3. **下载头像**
   ```bash
   node scripts/fetch-avatars-from-config.js
   ```

4. **检查结果**
   ```bash
   node scripts/check-avatars.js
   ```

### 方法二：手动添加图片

1. **下载图片**
   - 从 Wikimedia Commons、百度百科等网站下载
   - 保存到 `frontend/public/images/persons/`
   - 文件名格式：`{ID}-{姓名}.jpg`

2. **自动更新JSON**
   ```bash
   node scripts/add-avatar-urls.js
   ```

3. **检查结果**
   ```bash
   node scripts/check-avatars.js
   ```

---

## 📝 图片来源推荐

### 1. Wikimedia Commons（推荐）
- 网址：https://commons.wikimedia.org/
- 优点：版权清晰，图片质量高
- 使用方法：
  1. 搜索人物名称（中文或英文）
  2. 找到合适的图片
  3. 右键点击图片 → "复制图片地址"
  4. 将URL添加到配置文件

### 2. 百度百科
- 网址：https://baike.baidu.com/
- 使用方法：
  1. 搜索人物名称
  2. 查看词条中的图片
  3. 右键保存图片（注意版权）

### 3. 博物馆官网
- 中国国家博物馆
- 故宫博物院
- 各省市博物馆

---

## ⚠️ 注意事项

1. **版权问题**
   - 优先使用公有领域图片
   - 避免使用有版权声明的商业图片
   - 保留版权信息（如需要）

2. **图片要求**
   - 格式：JPG、PNG
   - 尺寸：建议 200x200 或更大
   - 大小：建议小于 100KB

3. **文件名格式**
   - 必须包含人物ID：`{ID}-{姓名}.jpg`
   - 例如：`8-秦始皇.jpg`

4. **网络问题**
   - 如果下载失败，检查URL是否正确
   - 某些网站可能有反爬虫，需要手动下载

---

## 🔧 故障排查

### 问题：下载失败
- 检查URL是否正确
- 检查网络连接
- 某些URL可能需要特殊处理

### 问题：图片不显示
- 检查文件路径是否正确
- 运行 `node scripts/add-avatar-urls.js` 更新JSON
- 检查文件名格式

### 问题：JSON未更新
- 确保图片文件已保存到正确目录
- 检查文件名是否匹配人物ID和名称
- 重新运行 `add-avatar-urls.js`

---

## 📊 当前状态

运行以下命令查看当前状态：
```bash
node scripts/check-avatars.js
```

---

## 💡 提示

- 建议先为重要人物（如秦始皇、汉武帝、唐太宗等）添加头像
- 可以分批添加，不需要一次性完成所有人物
- 定期运行 `check-avatars.js` 查看进度

