# ============================================
# 历史数据爬虫环境变量配置
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env (Linux/Mac) 或 copy .env.example .env (Windows)
# 2. 根据你的需求填写相应的环境变量
# 3. .env 文件已被 .gitignore 忽略，不会提交到版本库
#
# ============================================

# ============================================
# AI API 配置（二选一，推荐使用 Kimi）
# ============================================

# 选项 1: 使用 Kimi (Moonshot AI) - 推荐
# 获取 API Key: https://platform.moonshot.cn/
# 支持的模型: moonshot-v1-8k, moonshot-v1-32k, moonshot-v1-128k
KIMI_API_KEY=your-kimi-api-key-here
KIMI_MODEL=moonshot-v1-8k

# 选项 2: 使用 OpenAI
# 获取 API Key: https://platform.openai.com/api-keys
# 支持的模型: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_MODEL=gpt-4

# ============================================
# 爬虫行为配置（可选）
# ============================================

# 每个朝代最多爬取的人物数量（默认: 20）
# MAX_PERSONS_PER_DYNASTY=20

# 每个朝代最多爬取的事件数量（默认: 15）
# MAX_EVENTS_PER_DYNASTY=15

# 请求间隔时间（毫秒，默认: 2000，即 2 秒）
# 建议不要设置太小，避免被封 IP
# RATE_LIMIT_MS=2000

# ============================================
# 注意事项
# ============================================
# 
# 1. 如果没有配置 AI API Key，系统仍可运行，但会使用默认的人物和事件列表
# 2. Kimi API 通常比 OpenAI 更便宜，推荐使用
# 3. 请妥善保管你的 API Key，不要泄露给他人
# 4. 建议设置 API 使用限额，避免产生意外费用
#
# ============================================